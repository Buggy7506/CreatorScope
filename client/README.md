# CreatorScope Client

This directory contains the Vite + React frontend for CreatorScope.

## Setup

1. Install dependencies:
   ```bash
   cd client
   npm install
   ```
2. Run development server:
   ```bash
   npm run dev
   ```
3. Build for production:
   ```bash
   npm run build
   ```
